apiVersion: v1
kind: Service
metadata:
  name: {{ include "opscenter-backup.fullname" . }}
  labels:
    {{- include "opscenter-backup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opscenter-backup.selectorLabels" . | nindent 4 }}
